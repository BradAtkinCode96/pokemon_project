import json
with open(r"C:\Users\brada\OneDrive\Documents\GitHub\pokemon_project\pokedex.json") as file:
    mylist = json.load(file)

import pprint

MENU = ["Search by Name",
        "Search by ID",
        "Filter by Height, Weight and Speed",
        "Select Pokemon for your team",
        "View your team",
        "Clear the Team"
        ]

def pretty_print(lst):
    print("\n")
    for i in range(len(lst)):
        print(f"{i+1}. {lst[i]}")

def id_search():
    try:
        user_input = input("Enter the ID of the Pokémon: ")
        for pokemon in mylist:
            if pokemon["Id"] == user_input:
                print("Here is the match:")
                print(pokemon)
                return pokemon
        print("No Pokémon found with that ID.")
    except ValueError:
        print("Please enter a valid ID.")

def name_search(user_input):
    try:
        # user_input = input("Enter the name of the Pokémon: ")
        matches = []
        for pokemon in mylist:
            if user_input.lower() in (pokemon["Name"].lower()):
                matches.append(pokemon)
        if matches:
            print("Here are the matches:")
            for match in matches:
                pprint.pp(match)
            if len(matches) == 1:
                print("There is only pokemon that matches you search:")
                print(matches[0]["Name"])
                return matches
            else:
                print("Here are the matches:")
                print(match["Name"])
        else:
            print("No Pokémon found with that name.")
    except Exception as e:
        print(f"An error occurred: {e}")

def filter_by_type(mylist, type_to_filter):
    result = []
    for pokemon in mylist:
        try:
            if pokemon["Type 1"].lower() == type_to_filter.lower() or pokemon["Type 2"].lower() == type_to_filter.lower():
                result.append(pokemon)
        except ValueError:
            print("valid selection please")
    print(result)
    return result

def get_min_max():
    try:
        hmin = float(input("Enter minimum Height: "))
        hmax = float(input("Enter maximum Height: "))
        wmin = float(input("Enter minimum Weight: "))
        wmax = float(input("Enter maximum Weight: "))
        smin = float(input("Enter minimum Speed: "))
        smax = float(input("Enter maximum Speed: "))

    except ValueError:
        print("MUst be a valid number")    
    return hmin, hmax, wmin, wmax, smin, smax 

def filter_by_range(mylist, hmin, hmax, wmin, wmax, smin, smax):
    result = []
    for pokemon in mylist: 
        height = float(pokemon.get("Height (m)", 0))
        weight = float(pokemon.get("Weight (kg)", 0))
        speed = float(pokemon.get("Speed", 0))
        # print(speed, height, weight)
        if (smin <= speed <= smax and hmin <= height <= hmax and wmin <= weight <= wmax): 
            result.append(pokemon)
    pprint.pp(result)
    for i in result:
        print(i["Name"])
        print()
    return result

def move_display(alist): #plug in the list from previous function to allow move selection
    fullmoves = [] #fullmoves[0] is the name. Fullmoves[1]is the dictionary key with value["Description"]
    for pokemon in alist:
        x = pokemon["Moves"] #access the moves key(which is has a dictionary key)
        for i in x.items():
            y = i[0] #move name
            z = i[1]["Description"] #move description
            fullmoves.append(i) #
        for i in range(len(fullmoves)): ###move choice starts here
            print(f"{i+1}. {y}: {z}")
        select = int(input("Which moves would you like? (4 moves space-separated): ").split())
        print(select)
        for i in select:
            pass
    return fullmoves

# def move_choice(fullmoves):
#     for pokemon in fullmoves:
#         x = pokemon["Moves"] #access the moves key(which is has a dictionary key)
#         for i in x.items():
#             y = i[0] #move name
#             z = i[1]["Description"] #move description
#     for i in range(len(fullmoves)):
#         print(f"{i+1}. {y}: {z}")
#     select = int(input("Which moves would you like? (4 numbers space-separated): ").split())
    print(select)
    # for i in select:
    #     if i == 1


def add_to_team():
    p = input("Which pokemon would you like to add to your team?: ")
    match = name_search(p)
    if len(match) == 1:
        x = input(f"Would you like to add {p} to your team? y/n: ")
        try:
            if x == "Y" or "y":
                moves = move_display(match)
                return moves
            elif x == "N" or "n":
                return True
        except Exception as e:
            print(f"An error occurred: {e}")
    else: 
        print("More than one pokemon matches that search!")
def json_write(lst):
    with open(r'C:\Users\brada\OneDrive\Documents\GitHub\pokemon_project\team.json', 'r') as json_file:
        json.load(json_file)
        x = lst
        json.dump(x, json_file)


def team_view():
    with open(r'C:\Users\brada\OneDrive\Documents\GitHub\pokemon_project\team.json', 'r') as json_file:
        try:
            y = json.load(json_file)
            pprint.pp(y)
        except Exception:
            print("Your team is empty!")

def team_clear():
    with open(r'C:\Users\brada\OneDrive\Documents\GitHub\pokemon_project\team.json', 'r') as json_file:
        try:
            y = json.load(json_file)
            y.clear()
            json.dump(y, json_file)
        except Exception:
            print("Your team is now empty!")
    # print("Your team is now empty!")

print("Welcome to the Pokédex")
while True:
    try:
        pretty_print(MENU)
        choice = int(input("Please make a selection (0 to quit): "))
        if choice == 1:
            user_input = input("Enter the name of the Pokémon: ")
            name_search()
        elif choice == 2:
            id_search()
        elif choice == 3:
            hmin, hmax, wmin, wmax, smin, smax = get_min_max()
            filter_by_range(mylist, hmin, hmax, wmin, wmax, smin, smax)
        elif choice == 4:
            p = add_to_team()
            # move_choice(p)
        elif choice == 5:
            team_view()
        elif choice == 6:
            team_clear()
        elif choice == 7:
            # x = input("Pokemon")
            move_display(mylist) # x= move_display(mylist)
                                 # move_choice(x)
        elif choice == 0:
            print("Goodbye!")            
            break
        else:
            print("Invalid choice. Please choose a number between 0 and 2.")
    except ValueError:
        print("Invalid choice. Please enter a number.")